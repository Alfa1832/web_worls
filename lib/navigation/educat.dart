import 'package:flutter/material.dart';

class educate extends StatelessWidget {
  const educate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child:Text("Europian Education...")) ,
    );
  }
}
